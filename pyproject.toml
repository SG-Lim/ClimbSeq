[project]
name = "climbseq"
version = "0.1.0"
description = "A chunk-based asynchronous translator with sequence-level hill climbing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.2.0",
    "openai==2.6.1",
    "vllm==0.13.0",
    "aiohttp>=3.14.3",
]

[tool.uv]
override-dependencies = [
]
